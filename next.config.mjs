/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com", "images.unsplash.com"], // Allow images from these domains
  },
};

export default nextConfig;
